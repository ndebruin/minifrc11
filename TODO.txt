

- write DrivetrainController
    - handles only using some user joystick inputs during autoalign

- somehow modify inputParser to work for auto? (maybe??)
    - is there some other better way to do this????? 


*DO ALL THIS INDEPENDENT OF STATE MACHINE*
- tune autoalign control loops

*with state machine / much later*
- tune timeouts

- reset pose with desired position when selecting auto


// this autoaligns us to the nearest right branch
int closestIndex = GeoHelper::findClosestPointIndex(&odom.getPose(), AllRightBranches, nullptr);
Pose2D targetPose = GeoHelper::getBestApproachPose(&odom.getPose(), AllRightBranches[closestIndex]);

drivetrainController.startAutoalign(transformPose(transformPose(targetPose, armL3ForwardToRobotTransform), robotToSensorTransform));